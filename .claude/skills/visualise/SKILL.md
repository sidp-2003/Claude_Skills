---
name: visualise
description: Visualize data stored in the ".claude/skills/sfetchApi/data/" directory and geerates visualizations based on the data.Use this skill when you want to create visual representations of your data for better insights and analysis.
---

## Usage

### Step-1: Pick the python environemnt
Before you start, make sure to pick the python environment in which you want to run the code. You can run/install dependencies using '.venv' environemnt.

### Step-2: Run the python script to Visualize Data & Visualizations.
-You need use only this python script in the directory ".claude/skills/visualise/scripts/build_kpis.py".This script will generate visualizations based on the data you have and save them in the appropriate directory.




<!-- ### Step-2: Use pandas to build KPI
-You need to read the parquet files stored in the latest folder at ".claude/skills/migrate/data/".
-Afetr reading the dtata, you need to use pandas to build the following KPIs:
    - Total Sales
    - Total Returns
    - Net Sales
    - Average Sales per Store
    - Average Sales per product
    - Average Sales per Customer

### Step-3: Use matplotlib to visualise the KPIs
-After calculating the KPIs, you can use matplotlib to create visualisations for each KPI. You can create bar charts, line graphs, or any other type of visualisation that you think is appropriate for the data.

### Step-4: Save the visualisations
-Once you have created the visualisations, you can save the visualisations as PNG files in the folder with same name as source folder (datetime) ".claude/skills/visualise/visualization/" using plt.savefig() function. -->


<!-- After creating and looking at the script that it is working well you can remove step₹s 2,3,4 from the code and just keep the final code that reads the data, calculates the KPIs, visualises them and saves the visualisations. -->



